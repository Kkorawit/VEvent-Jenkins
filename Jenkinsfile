pipeline {
    agent any
    stages {
        stage('Update Datetime') {
            steps {
                script {
                    sh"docker ps"
                }
            }
        }
    }
}